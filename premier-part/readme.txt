1) Приложение не запускалось. Ошибка была в “models/index.js”,  при создании объекта new Sequelize передавалось некорректное колличество параметров, а именно (null, null, {[config]}). Посмотрев в документацию Sequelize, я изменил их и привел к виду ( ‘demo_name’, ‘root’, ‘password’, {[config]}). После этого приложение запустилось и открылось по ссылке http://localhost:3000.
    
2) Запрос get http://localhost:3000/graphql не проходил. Первым делом я решил проверить правильно ли я ввел URL, и посмотрел на midlewar’ у которая отвечает на запрос по данному URL, там я нашел опечатку (‘/graphgl’), исправив её я смог попасть в GraphQl IDE.
    
3) Ошибки в запросах.

3.1) Запрос mutation createUser принимал в input-параметры не все нужные поля, а именно не хватало avatarUrl: String!. После добавления этого поля в файл typeDefs.js, запрос выполнился.
    
3.2) Запрос query rooms возвращал список “переговорок” за исключением первой, исправить ошибку помогло удаление ‘offset: 1’ аргумента-объекта передаваемого в метод models.Room.findAll()
    
3.3) Опечатка в query.js в методе events(){ }  в findAll первым аргументом передавался псевдомассив arguments. Заменил на { }.
    
3.4) graphql/resolvers/index.js методы users() и room() ничего не возвращали. Добавил return в оба метода. 

3.5) В некоторых методах mutation.js на мой взгляд были стилистические неточности при возврате event через цепочки  промисов. Исправил.

3.6) Отсутствовал метод addUserToEvent(). Добавил.

3.7) Добавил usersIds: [ID], roomId: ID! в параметры к updateEvent 
